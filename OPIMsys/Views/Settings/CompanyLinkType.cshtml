@model IEnumerable<OPIMsys.Models.CompanyLinkType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "CreateCompanyLinkType")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditCompanyLinkType", new { id=item.CompanyLinkTypeId }) |
            @Html.ActionLink("Delete", "DeleteCompanyLinkType", new { id=item.CompanyLinkTypeId })
        </td>
    </tr>
}

</table>

@Html.ActionLink("Back to settings", "Index")
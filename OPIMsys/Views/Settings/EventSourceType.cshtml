@model IEnumerable<OPIMsys.Models.EventSourceType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "CreateEventSourceType")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditEventSourceType", new { id=item.EventSourceTypeId }) |
            @Html.ActionLink("Delete", "DeleteEventSourceType", new { id=item.EventSourceTypeId })
        </td>
    </tr>
}

</table>

@Html.ActionLink("Back to settings", "Index")